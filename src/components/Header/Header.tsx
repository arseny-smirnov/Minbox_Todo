import {Typography} from "@mui/material";
import './styles.css'

export const Header = () => {
    return (
        <div className="header">
            <Typography variant="h2">TODOS</Typography>
        </div>
    );
};